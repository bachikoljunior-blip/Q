# Reconstructed head registration v65 — closed reference pose

41 leaf types / 82 placements now have numeric author registration. The 18 selected small-part images and two canonical views were visually inspected. New dimensions are **author choices in HEAD_LOCAL_M**, not measurements from generated pixels. All prior images, v1 records, seven lower-face bicubic surfaces and six existing C1 seams stay unchanged. The unavailable old authored-v2 result is not represented as recovered.

The registry contains 277 one-source curves: skin171, optical2, brow8, hair spines24 and spine-derived hair perimeter72. The topology uses 52 skin faces, 118 skin nodes, three intentional skin boundary loops (two eye apertures and lower neck cut), with Euler characteristic−1. This is a combinatorial check, not a watertight or collision-checked mesh.

Each small face has an ordered trim loop, an interior guide/evaluator or retained control net, explicit UV correspondence, canonical tangent/normal frames and skin weights. The ear helix is an open rolled strip with two longitudinal trims and two ends, with normal-constrained transverse interpolation. F17 has one lower-lip ridge, the fixed F11 lower receiver and a separate closed-mouth contact curve. The contact is not a permanent upper/lower weld. E01/E02 use an authored 19.5mm-radius globe and 7mm optical seat; lids follow an offset sphere with 0.35mm **front-Z** offset, not a claim of constant radial clearance. The old flattened eye and translating lid are not silently adopted.

All24 hair roots evaluate on the same registered scalp trim/interior surface, with head weights, 0.6mm center support offset, matrices, axes, section widths/depths and unique spine/perimeter references. HT01 lift28mm and HT02 lift13mm explicitly resolve a shape distinction that the images alone do not quantify. The initial qualitative root targets were adjusted onto those scalp surfaces; their maximum 16.09mm optimization residual is target displacement, not an accepted support error. Twenty-one named over/under orders remain construction constraints; full strip overlap/intersection quality is not certified by root registration.

The first candidate gave different normals to separate curves meeting at the same skin node. The integrator identified this. The repair creates one normal contract per skin node, preserves native v63 normal constraints, and **changes authored endpoint tangent controls** to lie on that plane. Neck side curves become piecewise cubic; the collar-zone straight sections remain fixed. The independent verifier finds maximum incident tangent rank2, maximum tangent/normal dot1.56e−14 and maximum endpoint-normal difference1.21e−6 degrees. This corrects a real omission in the earlier33-check candidate; it does not turn a boundary graph into surface-quality acceptance. The266 authored endpoint adjustments include a maximum58.67° tangent change and are recorded individually.

The170 v1 relationships are all accounted for:111 numeric shared relations,4 replaced by point junctions,4 globe/lid sliding contacts,6 brow overlays,24 exact root supports and21 overlap orders. No generic unnamed filler was added. A02a is a selected blind concha bowl; deeper auditory canal completion is not claimed. Closed-mouth reference-pose work does not include open-mouth oral anatomy or all future facial performances.

## Neck / clothing boundary

`external-neck-contract.json` and `external-ports.json` carry the numeric handoff. Identity-bind chest→head translation is y−.760m; posed conversion must use matrices. C01/C02 retain chest y.560→.660, inner radii(.100,.086)→(.086,.083), outer(.104,.090)→(.090,.087), and front±12° opening.

The neck skin is (.094,.076) at HEAD y−.200 and (.078,.076) at y−.100, linear between these sections. The sampled same-ray static air gap is6–10mm over34037 angular/height samples. It is not a global closest-distance or posed-collision result. The lower skin cut at HEAD y−.210 / CHEST y.550 has radii(.098,.078),10mm below the collar lower rim. Four cut ports are open skin-continuation ports under clothing; one separate overlap port names cloth:C01/C02. No skin-to-cloth weld is implied.

## Verification and acceptance boundary

`verification.json`:36/36 finite checks pass, including exact IDs, closed loops, reciprocal one-source ownership, native byte/control preservation, normalized frames/weights, UV correspondences, root transforms and skin-node tangent-plane rank. Deterministic re-generation is recorded separately. The curve projection was visually inspected in front, profile and top views; it is not a shaded head or new model.

Remaining construction checks are new-patch Jacobian/folds, surface self-intersections, full hair-width contact/overlap, neck and eyelid deformation, pose clearance, likeness/materials and runtime cost. New mesh0, runtime0, remote0, new image0. Whole-person registration acceptance belongs to the integrator before any new mesh creation. PS4 full-screen quality, the fixed10-work comparison and2026-09-20 deadline remain unchanged and unproven; this numerical registration is not game completion.

The accepted worker is `/root/ultra_q_recover_reference_assembly/head_registration_v65`, formally assigned Ultra; effective internal reasoning is not publicly observable. An attempted separate Ultra ear subtask was rejected by the thread limit and created no worker; the assigned head worker completed that bounded work directly. All known tools are terminal, with no pending generation or remote operation. Measured wall time and unavailable phase splits are stated in `TIMING.json`; generator execution time is never labeled character-production time.
