# Head registration: partial, stopped, not accepted

Stopped on 2026-09-17 at the user's instruction: 「現在できたものを反映して停止して」.
This save preserves bytes already written when the stop instruction arrived. It does not complete, accept, regenerate, or newly validate the candidate.

## Saved baseline and partial delta

The previously delivered fixed baseline is `fd5e1335bc224b040aa923374807ddcc9ffe5883`.
The following four existing files have an unaccepted partial delta from that baseline:

- `review/head-registration-v65/generate.py`
- `docs/evidence/mira-assembly-v65/head-registration/registration.json`
- `docs/evidence/mira-assembly-v65/head-registration/external-neck-contract.json`
- `docs/evidence/mira-assembly-v65/head-registration/external-ports.json`

The partial changes introduce an oriented cross-product derivative for the boundary-Hermite disk recipe and evaluator, recalculate dependent hair-root placements, describe a replacement upper-neck Hermite recipe, and move the proposed neck cut to HEAD y = -0.280 m / CHEST y = 0.480 m. The cut is proposed as a garment-hidden free boundary with no receiver or cap. These are candidate numerical definitions, not accepted geometry.

## Unresolved findings and limits

- HREV-01: the fixed baseline's disk boundary orientation changed sign on 17 patches. The replacement derivative has not received the independent oracle review or full surface-regularity evaluation.
- HREV-02: the baseline's declared derivative magnitude differed from its evaluator, including a 2.31326 mm ROOT-H03 support discrepancy. The partial changes have not been independently checked for closure of this finding.
- HREV-03: the baseline's upper-neck linear loft disagreed with canonical boundary normals. The partial replacement recipe is incomplete as an evaluated surface contract; a complete neck surface evaluator and independent surface/normal check have not been produced.
- The proposed lower neck cut has no accepted proof of concealment by all relevant T01/T02/T04 garment surfaces. Removing the former unowned receiver from the candidate ports does not establish whole-assembly acceptance.

Before the stop instruction, the generator and the existing 36-check verifier were run once on the partial candidate. Those checks do not resolve these independent surface findings or establish garment concealment. No generator, verifier, plot regeneration, independent oracle, or new geometry evaluation is run as part of this stop save.

`HASHES.json`, `SOURCE_HASHES.json`, `REPRODUCIBILITY.json`, `verification.json`, `REPORT.md`, `TIMING.json`, and the registered-curves PNG/SVG remain historical evidence from the earlier work. They must not be treated as a matching validation or complete hash manifest for this partial candidate. In particular, the older hash manifest and changed generated artifacts are not reconciled in this stop save.

## Stopped state and resumption point

All local tool calls have terminated; pending tool IDs: none. No background task is left running. New mesh, runtime, and remote operations in this stop save: 0. Remote preservation belongs to the parent writer.

The prior recorded baseline wall time is 1803.14856 seconds. Separate repair-phase elapsed time was not captured and is not fabricated here. No timing value is a mesh-performance result.

If the user later resumes the task, begin by reviewing the saved partial delta against the baseline and the independent HREV-01/02/03 findings. The next acceptance work would require completing the neck surface definition, establishing actual surface/normal consistency, and independently checking the complete neck-cut concealment against the fixed clothing source. These are remaining tasks, not work authorized to continue during this stop.
