# 未列挙60型の原画像実見と局所view登録

前段 `8fa8879` の未列挙60型を、重複を除いた36枚の選択原PNGで実見した。頭26型/12枚、衣服34型/24枚。各シートの採用行だけに観察・表示view名・除外範囲を結び、120型/287配置/73選択原PNGの全行へ少なくとも一組の局所view列挙を揃えた。旧全体表と原画のbyteは変更していない。

36枚には各4～5の局所表示がある。表示数を実測済みカメラ方向数に読み替えず、`OBSERVATIONS.json` と `IMAGE_INSPECTION.json` に実際に判別できた面/方向の群と制限を併記した。「ラベルが文書にない」ことだけを資料不足や生成要求にしない。

| 対象 | 実見した範囲と残る限定 |
|---|---|
| 頭の一般patch | 前/外面、側面、裏/接合面、斜めの局所曲面。厚い裏縁・粒状の仮材質は採用しない。F07/F11のUNDERSIDEは狭い弧で、完全な裏面とはしない。 |
| A02a / F18 | 耳の凹面と三日月状境界を読める。深い耳道やsaddleの完成を画像だけで保証しない。 |
| N01/N02 | FRONT/REVERSEと2つのSIDE表示を実見。4表示・3つの定性的方向群で、90°と独立した3Qは保証しない。接合線・襟隙間は作者登録の対象。 |
| HT01/HT02など髪 | 根元/先端・外/内面・側面/斜めを参考にできる。類似したSIDEから根元liftや全24配置の重なりを測定しない。 |
| 衣服panel/帯 | 一枚の開放面、内外面、profile/斜めを局所参考にする。同一シートの旧L03/F03/F04等は混ぜない。C02の内面方向、TR05のprofileは厳密投影としない。 |
| F03 / F04 / F05 / B04 | 巻き帯、皿状brooch、輪枠、pinの外形は見える。巻き向き/終端・裏留め具・蝶番/接触寸法は未図示または未登録の範囲として残す。 |

今回36枚と前段7枚で、この担当の累積実見は43/73枚。残り30枚を新規実見したとは主張せず、元の明示policyと先行レビューに帰属させる。`allTypesHaveEnumeratedLocalViews=true` は120型の局所view追跡だけを示す。全imageの全ラベル確認、厳密投影、数値接合、全人物の組立成立を意味しないため、`allViewLabelsExplicitlyEnumerated` と `allReferencesAssemblyReady` はfalseを保持する。

新selection案は前段の全除外・全制限を保持する。TR02はこの有限単位では保存済みmethod-bのまま。衣服担当のv65-v2は別途生成・独立限定確認された候補として連絡を受けたが、確定commit/path/hash/policyの統合前に置き換えたとはしない。

`generate.py --check` で決定的再生成が一致し、検査は60型と36画像の全対応、120行・287配置・73画像の不変性、未実見型policyの不変性を確認。旧除外を削る/未描写の裏留め具viewを足す負の対照を両方検出した。文書の変更にゲーム・mesh・runtime試験は実施していない。

全体組立登録ゲートには、新TR02限定policyの結合、具体的な数値/本人左右/共有曲線/領域間接合の独立照合、全配置と完成像の整合が残る。局所の図数や自己採点から全組立受理を与えない。PS4写実全画面・固定10作品の比較・2026-09-20期限は不変。

新画像/数値設計/mesh/runtime/remote書込はこの担当で0。正式canonical taskは `/root/ultra_q_recover_reference_assembly/recover_reference_assets`。Ultra指定受理維持は親から受領済みで、内部実効強度は非公開。未完toolは0。
