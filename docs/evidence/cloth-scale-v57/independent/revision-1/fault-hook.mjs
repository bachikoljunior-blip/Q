import {registerHooks} from 'node:module';import {pathToFileURL} from 'node:url';import path from 'node:path';
const root=process.env.Q_REVIEW_ROOT,deps=process.env.Q_REVIEW_NODE_MODULES||path.join(root,'node_modules'),actor=pathToFileURL(root+'/src/actor-models.js').href,which=process.env.Q_REVIEW_FAULT||'none';
registerHooks({resolve(s,c,n){if(s==='three')return {url:pathToFileURL(deps+'/three/build/three.module.js').href,shortCircuit:true};if(s.startsWith('three/addons/'))return {url:pathToFileURL(deps+'/three/examples/jsm/'+s.slice(13)).href,shortCircuit:true};return n(s,c)},load(u,c,n){const r=n(u,c);if(u!==actor||which==='none')return r;let src=String(r.source),edit;if(which==='cape-uv')edit="if(family==='npc'){const uv=actor.cape.geometry.attributes.uv;uv.setX(0,uv.getX(0)+.2);}";else if(which==='npc-hair')edit="if(family==='npc')actor.g.traverse(n=>{if(n.material?.name==='Q cloth'&&n.material.color.getHex()===0xa7a397){n.material=n.material.clone();n.material.color.setHex(0xffffff);}});";else throw Error('Unknown deliberate fault');const needle='actor.animate({},0);return actor;';if(src.split(needle).length!==2)throw Error('Fault injection anchor is not unique');src=src.replace(needle,'actor.animate({},0);'+edit+'return actor;');return {...r,source:src};}});
