Correct HT01 and HT02 rows ONLY. They currently look like broad multi-lock wigs and root/tip labels contradict their taper. Each must be ONE simple cohesive small curved hair lock, like a slender tapered curved leaf with actual thickness, not multiple separate prongs. In FRONT: ROOT is the blunt BROADER end on IMAGE RIGHT (anatomical LEFT +X); centerline sweeps to IMAGE LEFT (anatomical RIGHT -X), ending in ONE tapered TIP on IMAGE LEFT. Label those exact ends ROOT/TIP consistently. HT01 has a clearly lifted root arc, HT02 has a broad but lower shallow curve. Four views must depict exactly those same ends and same object rotated; underside contains one small root attachment surface at the blunt end. Only a few fine aligned silver-gray strand grooves; no multiple side spikes or large fan. Keep both parts small relative to scalp locator, highlight one narrow lock each. Keep canonical hairstyle orientation and identity. HT03 row unchanged. Same neutral background/layout, no new types. No exact dimensions.
