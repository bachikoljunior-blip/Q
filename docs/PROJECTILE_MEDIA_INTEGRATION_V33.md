# Finite arrows and preserved media — integration v33

The loaded bow, flying arrow, live collision and danger forecast used different points and extents. The accepted arrow source now uses the tail/nock as the serialized position and the same 1.34 m extent for rendering and contact. The initial occupied span is tested before flight, including near walls and reflected arrows. A separate defect in the second draw cycle hid its loaded arrow because the previous shot's hit flag persisted; phase-aware visibility now preserves that second arrow.

Base is local `67baa205ac2dcc1bba8248884161b8df095b3f36`, preserved remotely as PR47 head `a94a081b7abcd145997dfe1ad6b44bcb34d27af9`, exact tree `6b428a13b051001b3e545d16765b952e6272c4e1`. Authors are projectile `b7af1cdcddce13c732de732c467f6448a9fc3f2d` and standalone representation `b46b649f96ef97f4bd37fc02dadb8d815ac26956`. Independent reviews are retained with both source units. The 25-minute projectile review exceeded its time box; that overrun is recorded, rather than replaced with an invented duration.

Independent arrow evidence covers actual Game two-shot cycles at 30/60/120 Hz, native Scene matrices, 35 contact/save/expiry cases, 60 bridge cases, 48-arrow batch forecasting and three meaningful failing source mutants. Same-frame native tail error is below 0.23 micrometres in the tested cases. Loaded skin/surface checks preserve intended distal-index/string contact close to the nock; these are finite triangle checks, not exhaustive intersection or rendered-quality proof. The ranger is 7,108 triangles / 12 mesh objects. Its 45 mm shaft and 200 mm head diameter remain exaggerated and are not a realism acceptance.

Existing serialized coordinates are retained, so the prior rendered arrow shifts forward 0.55 m to correct its old offset. Finite-tip contact occurs about 70.5 ms earlier in the fixed 12 m example; do not claim unchanged timing or balance. Reflections begin at the actual contact, with a zero-time test of the outgoing occupied span. Terrain span reuse is exact-position/velocity guarded and uses weak keys without adding saved fields. The independent counter includes work inside final exact contact: the same old 48-arrow fixture uses 1,618 actual terrain reads, compared with 1,522 now. The old reported 1,474 omitted 144 reads. Naive full-span replay uses 5,690 and fails the retained performance comparison. This is operation counting, not device FPS.

Ranger geometry intentionally changes, so the cloak regression fixture has an explicit composed ranger baseline: the accepted arrow geometry with the entire original pre-UV cloak function substituted from retained source. This is labelled as a composition, not a historical commit. The original v31 baseline and separate NPC baseline remain intact; all density, deformation and non-UV contract checks remain. Both geometry and animation provenance hashes were recomputed from the actual combined source. Five untracked initial-failure artifacts in the author's original worktree are retained without deletion; they match preserved failed-candidate evidence, are not imported here and have no identified reappearance cause. Only committed author source was integrated.

All **25 required commands / 299 tests** passed in **148.485818 seconds**. Actual combined initial JS is **164,988 B**, under the existing 165,000 B transfer guard, with three chunks. Standalone candidate is **15,498,171 B**, SHA256 `a09c7ad2ac71e1e703c7435116ff32a8a83953b5b5ff24a91acaf3c24ab7fd8e`. Fixed stage is `/workspace/scratch/e72662e3b71f/Q-ps4-v33/artifacts/site-axnjK6`. All original 27 media files are byte-identical through source, Vite, stage and optional representation. [Combined validation](evidence/projectile-media-v33/validation.json).

The standalone representation preserves original media bytes while reducing text overhead. Its independent same-source measurements also show a material additional Node title/launch CPU cost. The implementation and those costs are preserved as an optional delivery candidate; they are not a requirement for the normal multi-file Site or a device-memory improvement.

The latest direct user proposal is to reconsider how the game is played if capacity is the obstacle. This supersedes treating a single HTML's 16 MiB Git-data publishing guard as the art budget of the entire game. The existing owner-only URL already serves separate files. A new finite unit makes that normal URL the primary delivery contract, verifies every emitted/staged file, and separates optional offline HTML generation from the ordinary release gate. It will compare phase-specific loading using the same media, rather than silently reducing image quality or assigning the decoder's CPU cost to every player. The initial JS guard is explicitly a tunable transfer regression check; its remaining 12 B is not hardware capacity. No threshold is changed in this checkpoint. Native/installable alternatives and their additional device/distribution work are assessed separately; no new service, payment or audience change is authorized.

The actual GitHub main remains `547676fdce8d8e97abed9f065aad0b6e24af2fd6`; PR39 remains open/unmerged after the unresolved 03:14Z normal-merge timeout. There is no new merge mutation, actual main integration or Site publication in this unit. Site23 remains owner-only. The September 20 full-screen PS4 target is still unsupported by rendered/device/touch/listening/blinded-comparison evidence and delivery latency. The missing supervised preview is not bypassed. Continue the actual delivery-method change, nondegenerate grounded cloth motion and suitable character materials; do not treat this source checkpoint or tests as attainment.
