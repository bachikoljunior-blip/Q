# Retired procedure documents — 2026-08-03

Removed from force by user instruction. Kept as the origin record for decisions made
under them; **not** in force, and finding a rule here is not grounds to reinstate it.

| File | What it was |
|---|---|
| `PROTOCOL.md` | the nine-item mandatory floor, adaptive rigor tiers, lifecycles, planning, review, gates, completion |
| `START_HERE.md` | boot loader: reading order, compressed floor, resume procedure |
| `PROJECT_OPERATING_PROTOCOL.md` | superseded stub pointing at the above |
| `MODULES/` | nine optional procedure modules, none ever activated |

What governs now is the block at the head of `AGENTS.md`: the goal, how elements and their
references are derived, how a blind comparison decides an element, and what a unit of work is.
**Everything else about method is the worker's own.**

The prohibitions the protocol carried that were never procedure — never claim an unfinished
action, no secrets, never build a reference's content into the game, a criterion is weakened
only on proof it is unreachable, no unattended chaining — survive in `AGENTS.md`.
