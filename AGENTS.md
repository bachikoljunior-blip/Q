# Project loader

At the start of every Work or Codex run for this project, read `START_HERE.md`, load the minimum relevant durable state, verify it against actual project reality, and resume from the last verified checkpoint. Apply the mandatory floor in full and the lowest sufficient rigor above it. Load protocol detail and module files only when active work reaches them.

At the end of every project-changing Work or Codex session, update the canonical continuation state, execute the applicable checks, integrate the newest runnable checkpoint into `main`, publish it through the established GitHub Pages workflow, and verify the served revision and primary journey. The overall objective may remain incomplete, but its gaps must remain explicit. Do not stop at a local commit, branch, or open pull request unless a mandatory check, blocking regression, safety or licensing issue, permission, branch protection, or required human approval prevents delivery; preserve the exact blocker and resumption action when that happens. This run-level delivery checkpoint does not end the logical session, which still requires the user's explicit declaration. End each project-changing run with the required floor check line.
