# 2026-08-29 — full execution pass

## User directive now treated as permanent

- 進められることを途中報告で止めない。
- 弱点を発見したら、同じ作業サイクル内で中止・修正・再調査・実装・検証・記録まで進める。
- 「次にやる」で返答を終えない。
- 既に回答済みの条件や指摘を再質問しない。
- 人間にしかできない入力、認証、実取引、第三者利用だけを明確な境界として残す。

正本は `AGENTS.md` と `PROJECT_STATE.md`。

## Completed in this pass

1. 最新mainをfresh cloneし、EXP004が公開中であることを確認。
2. JavaScript構文、字幕実験ではなくFBA監査本体であること、静的ファイル、XML/JSON/HTMLを検査。
3. `tests/audit-core.smoke.js` を実行し、引用符付きCSV、日本語見出し、数値処理、保守的な理由分類、取消処理、原価注意表示を検証。
4. ローカルHTTPとGitHub Pagesの公開URLでEXP004を確認。
5. GitHub ActionsのスモークテストとPagesデプロイ成功を確認。
6. IndexNow所有キーと自動送信workflowを追加。公開対象ページを検索エンジンへ自動通知するようにした。
7. 匿名ファネル指標を毎日取得し、課金開発のゲートを自動判定するworkflowを追加。
8. `metrics/latest.json` と `metrics/latest.md` の自動スナップショット生成・保存を確認。
9. IndexNow、メトリクス、スモークテスト、Pagesデプロイの最新workflow成功を確認。
10. 公開IndexNowキーがGitHub Pagesから取得できることを確認。

## Current truth

EXP004は売れると決め打ちしていない。現在は無料の反証実験。

有料版、SP-API接続、重い機能、決済は次の証拠が揃うまで作らない。

- ユニーク端末100
- 監査実行30
- 出力またはケース作成10
- 月額利用意向5
- 実在するAmazon.co.jpレポートとの互換性
- 深刻な誤検出がないこと

母数到達後に基準未達なら、課金機能を作らずEXP004を本命から降格する。この判定は `metrics/latest.md` に自動で残る。

## Human-only boundary

コード、公開、検査、検索通知、匿名計測、ゲート判定までは自動化済み。

こちらだけでは生成できない証拠は、第三者による実利用、実在レポートとの互換性、実際の補填差額、支払意思、売上。この外部証拠を捏造して開発継続の理由にはしない。
