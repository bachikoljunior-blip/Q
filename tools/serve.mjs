import{createReadStream,statSync}from'node:fs';import{createServer}from'node:http';import{extname,join,normalize,resolve}from'node:path';import{fileURLToPath}from'node:url';
const ROOT=resolve(fileURLToPath(new URL('../',import.meta.url))),M={'.html':'text/html; charset=utf-8','.js':'text/javascript; charset=utf-8','.mjs':'text/javascript; charset=utf-8','.css':'text/css; charset=utf-8','.json':'application/json; charset=utf-8','.webmanifest':'application/manifest+json; charset=utf-8','.svg':'image/svg+xml'};
export function serve({port=Number(process.env.Q_PORT||4173)}={}){const server=createServer((req,res)=>{const u=new URL(req.url||'/',`http://${req.headers.host||'localhost'}`);let p=decodeURIComponent(u.pathname);if(p==='/Q'||p==='/Q/')p='/index.html';else if(p.startsWith('/Q/'))p=p.slice(2);if(p.endsWith('/'))p+='index.html';p=normalize(p).replace(/^(\.\.(\/|\\|$))+/,'').replace(/^[/\\]+/,'');const file=join(ROOT,p||'index.html');if(!file.startsWith(ROOT)){res.writeHead(403).end();return}try{if(!statSync(file).isFile())throw 0;res.writeHead(200,{'content-type':M[extname(file)]||'application/octet-stream','cache-control':'no-store'});createReadStream(file).pipe(res)}catch{res.writeHead(404,{'content-type':'text/plain'}).end('Not found')}});return new Promise((ok,no)=>{server.once('error',no);server.listen(port,'127.0.0.1',()=>ok(server))})}
if(process.argv[1]===fileURLToPath(import.meta.url)){const s=await serve();console.log(`http://127.0.0.1:${s.address().port}/Q/`)}
